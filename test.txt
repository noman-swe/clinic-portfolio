// app/about/components/Banner.tsx
import Image from "next/image";
import Breadcrumb from "@/components/shared/Breadcrumb"; // Adjust path as needed

interface BannerProps {
    data: {
        title: string;
        subtitle: string;
        bgImage: string;
    };
}

export default function AboutBanner({ data }: BannerProps) {
    // Define dynamic steps for the About page
    const breadcrumbSteps = [
        { label: "About Us" } // You can add intermediate parents here if nested
    ];

    return (
        <section className="font-sans">
            {/* Hero Content Section */}
            <div className="relative isolate overflow-hidden">
                {/* Background */}
                <Image
                    src={data.bgImage}
                    alt={data.title}
                    fill
                    priority
                    className="object-cover"
                />

                {/* Overlay */}
                <div className="absolute inset-0 bg-gradient-to-r from-[#041C3F]/95 via-primary/85 to-primary/70" />

                {/* Decorative Blur */}
                <div className="absolute -top-32 left-20 h-72 w-72 rounded-full bg-sky-500/20 blur-[120px]" />
                <div className="absolute bottom-0 right-20 h-72 w-72 rounded-full bg-indigo-500/20 blur-[120px]" />

                <div className="relative z-10 container mx-auto px-6 md:px-8 py-24 md:py-32">
                    <div className="max-w-3xl">
                        {/* Small Label */}
                        <span className="inline-flex rounded-full border border-sky-400/30 bg-white/10 px-4 py-1 text-xs sm:text-sm font-semibold tracking-wider text-sky-300 backdrop-blur uppercase">
                            About Company
                        </span>

                        {/* Heading */}
                        <h1 className="mt-5 text-3xl font-bold leading-tight text-white md:text-5xl lg:text-6xl tracking-tight">
                            {data.title}
                        </h1>

                        {/* Accent Line */}
                        <div className="mt-5 h-1 w-20 rounded-full bg-sky-400" />

                        {/* Subtitle */}
                        <p className="mt-6 max-w-2xl text-base md:text-lg leading-relaxed text-slate-300 font-light">
                            {data.subtitle}
                        </p>
                    </div>
                </div>
            </div>

            {/* Dynamic White-bg Breadcrumb Navigation Bar */}
            <Breadcrumb steps={breadcrumbSteps} />
        </section>
    );
}